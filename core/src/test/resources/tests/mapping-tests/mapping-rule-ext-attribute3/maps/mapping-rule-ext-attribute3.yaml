---
mapping:
  rules:
  - remote:
    - path: "'PT'"
    - path: "'AWSPolicy'"
    local:
      user:
        domain: "{D}"
        foo:
          value: "{0}2H"
          type: xs:dateTime
        name: "{D}"
        email: "{D}"
        roles: "{D}"
        expire: "{D}"
      faws:
        policy: "{1}"
  - local:
      user: {}
      faws:
        policy: AWSPolicy2
  version: RAX-1
