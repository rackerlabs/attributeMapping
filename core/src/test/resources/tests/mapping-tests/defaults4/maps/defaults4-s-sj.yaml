---
mapping:
  rules:
  - local:
      user:
        domain: "{D}"
        name: "{D}"
        email: "{At(FirstName)} {At(LastName)} <{D(name)}@{D(domain)}.rackspace.com>"
        roles: "{D}"
        expire: "{D}"
  version: RAX-1
  namespaces:
    foo: bar
