---
mapping:
  rules:
  - local:
      user:
        domain: "{D}"
        name: "{D}"
        email: "{D(name)}@rackspace.com"
        roles: "{D}"
        expire: "{D}"
  version: RAX-1
  namespaces:
    foo: bar
