---
mapping:
  rules:
  - local:
      user:
        ext:
          value:
          - "{Ats(ext2)}"
          multiValue: true
        domain: "{D}"
        name: "{D}"
        email: "{D}"
        roles: "{Ats(roles)}"
        expire: "{D}"
      extGroup:
        ext2:
          value: "{Ats(ext2)}"
          multiValue: true
  version: RAX-1
